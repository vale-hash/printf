#ifndef MAIN_H
#define MAIN_H

#include <stdarg.h>
#include <unistd.h>

int _strlen(char *s);
int _putchar(char c);
int _printf(const char *format, ...);
void puts_str(char *str);
void printNum(int num);
#endif
