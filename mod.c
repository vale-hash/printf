#include "main.h"
/**
 * mod - function that print the lenghtnof a string
 * @list: the string inputed
 * Return : returns %.
 */


int mod(va_list list)
{
	char c = '%'

	void list;	

	write(1, &c, 1);
	return (1);
}
